# third_party

This folder is reserved for local dependency drops only.

- Do not commit NVIDIA binaries.
- Expected future layout:
  - `third_party/nvidia/cuda/<version>/<platform>/`
  - `third_party/nvidia/tensorrt/<version>/<platform>/`

